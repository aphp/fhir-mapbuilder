<!--
  The PR title becomes the subject of the merge commit and feeds release
  automation, so it must itself be a valid Conventional Commit
  (type: feat | fix | test | docs | chore | build, subject <= 72 chars, no
  trailing period). Example: fix(validation): reject StructureMap without a group
-->

## What and why

<!-- What does this change do, and why is it needed? Link the issue it closes. -->

Closes #

## Checklist

- [ ] Every commit is signed off (`git commit -s`, DCO) and is a valid Conventional Commit — header <= 72 chars, no trailing period.
- [ ] Branch history is clean: rebased on the latest `main`, fixup/WIP commits squashed.
- [ ] `npm test` (in `vscode-extension`) and `mvn verify` (in `fhir-mapbuilder-validation`) pass locally.
- [ ] If this bumps FHIR or Matchbox: `fhir.core.version` and `health.matchbox:matchbox-engine` were bumped together and stay compatible.
- [ ] No `CHANGELOG` entry added — the changelog and version bump are generated by release automation.
